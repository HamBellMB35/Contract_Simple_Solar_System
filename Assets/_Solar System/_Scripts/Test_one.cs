using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test_one : MonoBehaviour
{
    int testInt = 0;
    // This is a test script to check if the script is working or not. It doesn't do anything.
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
